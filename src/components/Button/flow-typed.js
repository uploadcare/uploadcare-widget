/* @flow */
export type Props = {
  className?: string,
  withIcon?: string,
  isMuted?: boolean,
  isOverlay?: boolean,
  isPrimary?: boolean,
  size?: string,
  title?: string,
  children?: string,
}
