/* @flow */
export type Props = {
  className?: string,
  isFake?: boolean,
  withIcon?: string | boolean,
  isMuted?: boolean,
  isOverlay?: boolean,
  isPrimary?: boolean,
  size?: 'big' | 'small',
  title?: string,
  type?: 'button' | 'submit',
  caption?: string,
}
