.uploadcare--panel__powered-by {
  background: var(--color-base);
  color: rgba(#fff, 0.5);

  @media (--desktop) {
    display: none;
  }

  .tab:not(:nth-child(2)).tab_current ~ & {
    display: none;
  }
}
