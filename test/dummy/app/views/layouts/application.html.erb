<!DOCTYPE html>
<html>
<head>
    <title>Dummy</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=.25, minimal-ui" />
    <%= csrf_meta_tags %>

    <%= yield :javascript_includes %>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <% if content_for?(:javascript_base_lib) %>
        <%= yield(:javascript_base_lib) %>
    <% else %>
        <%= javascript_include_tag "uploadcare/build/uploadcare.full" %>
    <% end %>
    <script>
        uploadcare = uploadcare.__exports;
    </script>

    <script>
    var $ = uploadcare.jQuery;
    var jQuery = uploadcare.jQuery;

    UPLOADCARE_PUBLIC_KEY = 'demopublickey';
    UPLOADCARE_SCRIPT_BASE = '/assets/uploadcare/';
    UPLOADCARE_TABS = 'default huddle';
    UPLOADCARE_DEBUG_UPLOADS = true;
    UPLOADCARE_MULTIPART_MIN_SIZE = 10 * 1024 * 1024;

    // UPLOADCARE_PUBLIC_KEY = '5248878573444a0016c3';
    // UPLOADCARE_SOCIAL_BASE = 'http://localhost:5000/';
    // UPLOADCARE_URL_BASE = 'http://webclient.lacolhost.com:8000/upload/';
    // UPLOADCARE_CDN_BASE = 'http://robots.lacolhost.com:8888/';
    // UPLOADCARE_LOCALE = 'lv';
    </script>
    <%= yield :head %>
</head>
<body>

<%= yield %>

</body>
</html>
