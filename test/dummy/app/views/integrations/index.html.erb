<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>

<pre>
    var myButton = $('#some-button');
    myButton.click(function() {
        uploadcare.widget.showDialog().done(function(uploader) {

            uploadcare.jQuery(uploader).on('uploadcare.api.uploader.load', function() {
                alert(this.fileId);
            });
            uploader.upload();


        });
    });
</pre>

<script>
    $(function() {
        var myButton = $('#some-button');
        myButton.click(function() {
            uploadcare.widget.showDialog().done(function(uploader) {

                uploadcare.jQuery(uploader).on('uploadcare.api.uploader.load', function() {
                    alert(this.fileId);
                });
                uploader.upload();


            });
        });
    });
</script>

<button id="some-button">hey</button>
