@import '_config.pcss';

.panel__or-text {
	margin: 15px 0;

	font-size: 10px;
	font-weight: 300;
	line-height: 1;

	text-align: center;
	text-transform: uppercase;

	color: var(--gray-155);

	@media (--desktop) {
		& {
			margin: 20px 0;

			font-size: 15px;
		}
	}
}
