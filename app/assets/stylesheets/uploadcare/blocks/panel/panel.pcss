.panel {
  flex-shrink: 0;
  flex-basis: auto;

  display: flex;
  flex-direction: column;

  box-sizing: border-box;

  width: 100%;
  height: 500px;

  border: 1px solid var(--color-primary);

  @media (--desktop) {
    flex-direction: row;
  }
}
