.panel {
  flex-shrink: 0;

  display: flex;
  flex-direction: column;

  position: relative;

  box-sizing: border-box;
  overflow: hidden;

  width: 100%;
  height: 500px;

  border: 1px solid var(--color-primary);

  @media (--desktop) {
    flex-direction: row;
  }
}
