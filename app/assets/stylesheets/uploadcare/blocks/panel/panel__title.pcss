@import '_config.pcss';

.panel__title {
	max-width: 350px;

	margin: 15px 0;

	font-size: 20px;
	line-height: 1.1;

	text-align: center;

	color: var(--gray-69);

	@media (--desktop) {
		& {
			margin: 20px 0;

			font-size: 35px;
		}
	}
}
