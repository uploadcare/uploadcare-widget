@import '_config.pcss';

.panel__header {
  flex: 0 0 50px;

  padding: 0 20px;

  overflow: hidden;

  @media (--desktop) {
    & {
      flex-basis: 70px;

      padding-left: 60px;
      padding-right: 60px;
    }
  }
}
