.file__name {
  overflow: hidden;

  margin: 0;

  max-width: 200px; /* TODO Hack for limit block if content has long string without spaces */

  line-height: 25px;

  white-space: nowrap;
  text-overflow: ellipsis;

  .file_error & {
    color: rgba(255, 0, 0, .6);
  }
}
