.file {
	display: flex;
	flex-direction: row;
	align-items: center;
	justify-content: space-between;

	box-sizing: border-box;

	cursor: default;

	margin-bottom: 20px;

	&:hover, &:focus {
		background: #f5f5f5;
	}

	@media (--desktop) {
		& {
			margin: 0;
			padding: 10px;
		}
	}
}
