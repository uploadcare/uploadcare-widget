.draganddrop {
  .dragging & {
    margin: 20px;

    background: var(--back-main-color);
    color: var(--fill-main-color);
    border: 2px dashed;

    border-radius: 20px;
  }

  &.dragging {
    color: var(--color-primary) !important;
  }
}
