.button_muted {
  border-color: transparent !important;
  color: var(--fill-main-color);

  border-radius: 0;

  &:hover,
  &:focus {
    color: var(--color-primary);
  }

  &:disabled,
  &[aria-disabled=true] {
    border-color: transparent !important;
  }
}
