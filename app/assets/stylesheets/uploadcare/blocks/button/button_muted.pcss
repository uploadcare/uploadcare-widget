.button_muted {
  border-color: transparent;
  border-radius: 0;

  &,
  &:disabled:hover,
  &:disabled:focus,
  &:disabled:active {
    color: var(--fill-main-color);
  }

  &:hover, &:focus {
    color: var(--color-primary);
  }
}
