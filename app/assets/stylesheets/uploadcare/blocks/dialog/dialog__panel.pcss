.dialog__panel {
  flex: 1 0 100%;
  display: flex;
  flex-direction: column;

  width: 100%;

  @media (--mobile) {
    & {
      box-sizing: border-box;

      padding-top: 60px;
    }
  }

  @media (--desktop) {
    & {
      flex-direction: row;
    }
  }
}
