@import '_config.pcss';

.dialog__close {
	position: absolute;
	top: 0;
	right: 0;
	z-index: calc(var(--z-index) + 1);

	width: 60px;
	height: 60px;

	background-color: red;

	cursor: pointer;
	text-indent: -9999px;
}
