.link {
  &,
  &:link,
  &:visited {
    cursor: pointer;

    color: var(--color-primary);

    text-decoration: underline;
  }

  &:hover, &:focus {
    color: var(--color-primary-light);

    text-decoration: none;
  }

  &:focus {
    outline: color(var(--color-primary) a(50%)) 2px solid;
    outline-offset: 1px;
  }

  &:active {
    color: var(--color-primary-dark);
  }
}
