@import '_config.pcss';

.menu__container {
	width: calc(100% + 40px);  /* Hack for hide scrollbar */
	height: 100%;

	overflow-y: auto;
	overflow-x: hidden;

	font-family: var(--font-family);
	font-size: 15px;
}
