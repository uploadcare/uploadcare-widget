.file-sources {
  flex-shrink: 0;

  display: flex;
  flex-direction: column;
  align-items: center;

  width: 100%;

  @media (--desktop), (--mobile) and (orientation: landscape) {
    & {
      display: none;
    }
  }
}
