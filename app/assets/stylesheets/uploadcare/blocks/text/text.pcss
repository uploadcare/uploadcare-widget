.text {
  margin: 10px auto;
  padding: 0;

  font-family: var(--font-family);
  font-weight: 300;
  font-size: 17.5px;
  line-height: 20px;

  & + .text {
    margin-top: 5px;
    margin-bottom: 5px;
  }
}
