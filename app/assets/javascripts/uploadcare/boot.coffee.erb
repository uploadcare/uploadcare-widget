# = require ./namespace.coffee

uploadcare.version = '<%= UploadcareWidget::VERSION %>'
uploadcare.jQuery = jQuery

{expose} = uploadcare

expose('version')
expose('jQuery')

expose 'plugin', (fn) ->
  fn(uploadcare)
